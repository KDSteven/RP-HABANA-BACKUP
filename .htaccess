# === Security: Protect sensitive files ===

# Deny access to .env and other hidden files
<FilesMatch "^\.env">
  Require all denied
</FilesMatch>

# Optionally deny access to config files (like db.php)
<FilesMatch "^(config\.php|db\.php|env\.php)$">
  Require all denied
</FilesMatch>

# Disable directory listing
Options -Indexes

# === Optional: Enable rewrite engine (if you use clean URLs) ===
# RewriteEngine On
# RewriteBase /RP-HABANA/
